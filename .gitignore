# Source materials — problem statement, wireframes, references. Not shipped.
materials/

# Per-builder local AI instructions. Never committed (see docs/BUILD_RULES.md).
CLAUDE.md
AGENTS.md
.claude/

node_modules/
dist/
.env
.env.local
*.local
.vercel
.DS_Store
npm-debug.log*

# Real env files and anything downloaded into the repo. `.env.example` is
# committed on purpose and must stay matchable, so this is not a blanket
# `.env*` — that would shadow the example everyone copies from.
.env.local
.env.*.local
*.download
